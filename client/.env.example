# Client-side env vars — copy this file to client/.env to override defaults
# All client-exposed vars MUST be prefixed with VITE_ (Vite security requirement)

# URL of the backend server. Defaults to http://localhost:3000 if unset.
VITE_SERVER_URL=http://localhost:3000
